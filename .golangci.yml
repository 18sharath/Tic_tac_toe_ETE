
version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - unused


linters-settings:
  errcheck:
    check-type-assertions:true

issue:
  max-issue-per-linters: 0
  max-same-issue: 0
  uniq-by-line: true
  new: false
